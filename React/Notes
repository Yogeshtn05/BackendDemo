Identifiers:
    1.class Identifiers(.)
    2.Id Identifiers(#)
    3.Tag Identifiers(elements)
    4.Universal Identifiers(*)
    5.group Identifiers(class,tag,..)
    6.Attribute Identifiers([attr])

List:
    1.Ordered List
    2.Undordered List
    3.Discriptive List

DOM:
    1.Basic DOM(HTML)=>(when any modification is performed in any tag it will refresh whole UI)
    2.Virtual DOM(react)=>(when any modification is performed in any tag it will refresh particular tag UI only)

EventHandling:
    1.useState
    2.useEffect(Identify the Alteration in the function with the given variable)
    3.useRef(It can be Rerendering in the particular component only,Ensure useState will Rerendering the whole file)
    4.useContext and createContext(Passing values as exporting. It can usable in child of anywhere incomponents)
    5.useMemo(It can be used for applying function to particular function only)


    //let a= 4>3 ? 6>8 ? 10: 3>8 ? 4>1 ? 20 :30:4>7? 40:50:6>8?60:70;


Backend :
	1. npm init -y
	2. npm i express
	3. npm i nodemon
    4. npm i cors